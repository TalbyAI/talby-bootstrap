# File direct install single artifact

Purpose: show the minimal successful install of one artifact from a `file:` source.

Represented command: `tbboot install file:local-example-source --artifact base-readme`

Kind: `atomic-case`

Polarity: `positive`

Normative outputs: `expected/exit-code.txt`, `expected/stdout-contains.yaml`, `expected/consumer/`
