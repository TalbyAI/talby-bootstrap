# JSON success envelope minimal

Purpose: prove the minimal successful JSON output contract.

Represented command: `tbboot --output json install file:local-example-source --artifact base-readme`

Kind: `atomic-case`

Polarity: `positive`

Normative outputs: `expected/exit-code.txt`, `expected/stdout.json`
