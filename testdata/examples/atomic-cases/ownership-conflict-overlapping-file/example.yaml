schema_version: 1
id: ownership-conflict-overlapping-file
kind: atomic-case
status: broken
polarity: negative
summary: Reject materialization when two managed artifacts claim the same file.
commands:
  - argv:
      - tbboot
      - install
      - file:conflicting-source
verification:
  exit_code: exact
  stdout_text: absent
  stdout_json: absent
  stderr_text: contains
  stderr_json: absent
  consumer_state: exact
normative_outputs:
  - expected/exit-code.txt
  - expected/stderr-contains.yaml
  - expected/consumer
tags:
  - ownership-conflict
  - materialization
  - negative
