# Declare only flow

Purpose: show a positive `--declare-only` flow that updates only the manifest and leaves materialized files untouched.

Represented command: `tbboot install file:local-example-source --declare-only`

Kind: `scenario`

Polarity: `positive`

Normative outputs: `expected/exit-code.txt`, `expected/stdout.txt`, `expected/consumer/`
