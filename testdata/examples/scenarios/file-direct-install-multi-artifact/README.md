# File direct install multi artifact

Purpose: show a positive direct install from a `file:` source that resolves two artifacts and writes the expected consumer state.

Represented command: `tbboot install file:local-example-source`

Kind: `scenario`

Polarity: `positive`

Normative outputs: `expected/exit-code.txt`, `expected/stdout.txt`, `expected/consumer/`
